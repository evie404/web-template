type OS {
    id: ID!
    name: String!

    phones: [Phone!]

    createdAt: Time!
    modifiedAt: Time!
}

input OSInput {
    name: String!
}

type OSMutation {
    createOS(phone: OSInput!): OS!
}
