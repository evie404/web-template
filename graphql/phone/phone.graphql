type Phone {
    id: ID!
    name: String!

    os: OS!
    manufacturer: Manufacturer!

    createdAt: Time!
    modifiedAt: Time!
}

input PhoneInput {
    name: String!

    osID: ID!
    manufacturerID: ID!
}

type PhoneMutation {
    createPhone(phone: PhoneInput!): Phone!
}
